Options +FollowSymLinks -MultiViews
DirectoryIndex index.html

RewriteEngine On

# Ensure trailing slash for directory index routing
RewriteCond %{REQUEST_FILENAME} -d
RewriteCond %{REQUEST_URI} !(.*)/$
RewriteRule ^(.*)$ /$1/ [L,R=301]
